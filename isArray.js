const friend = ['hi', 'hello', 'ok', 'what'];

const food = [12, 3, 34, 5, 54];

const age = 13;

const foods = ' ro so go ll a';

console.log(Array.isArray(friend));
console.log(Array.isArray(food));
console.log(Array.isArray(age));
console.log(Array.isArray(foods));